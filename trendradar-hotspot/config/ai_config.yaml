# ============================================================
# TrendRadar AI 绘画监控 · LLM 配置
# 模型: FreellmAPI (localhost:3001, 已验证 Bearer token 连通)
# 测试时间: 2026-07-24 19:28 — 返回 200 "Hi there!"
# ============================================================
ai:
  # model=auto 让 API 自动路由模型
  model: "openai/auto"

  # Bearer token 方式验证通过
  api_key: "freellmapi-7a734b7fb7f6106f666c6203186158737fedcaa9c5da8b58"

  # 自定义 API 基地址
  api_base: "http://localhost:3001/v1"

# AI 深度分析
ai_analysis:
  enabled: true
  language: "Chinese"
  mode: "follow_report"
  max_news_for_analysis: 150
  include_rss: false
  include_standalone: true
  include_rank_timeline: true

# AI 智能筛选
ai_filter:
  min_score: 0.6
  batch_size: 200
  batch_interval: 2

# AI 翻译
ai_translation:
  enabled: false
